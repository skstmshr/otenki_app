name: flutter_otenki_app
description: A new Flutter application.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  hooks_riverpod: ^0.10.1
  flutter_hooks:
  geolocator: ^6.0.0+4
  http:
  cupertino_icons: ^0.1.3
  xml:
  google_fonts: ^1.1.0
  freezed_annotation:
  permission_handler:
  connectivity:
  url_launcher: ^5.7.1
  flutter_dotenv: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  json_serializable:
  freezed: ^0.11.6
  build_runner: 1.10.1

flutter:
  uses-material-design: true

  assets:
    - images/
    - secret.env